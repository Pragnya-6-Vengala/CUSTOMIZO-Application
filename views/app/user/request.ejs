<%- include("../../header") -%>

    <link rel="stylesheet" href="/stylesheets/app/request.css">
    <div class="d-flex justify-content-center">
        <h5>Create Form</h5>
    </div>
    <div class="d-flex justify-content-center card-container">
        <form class="col-12 col-md-6 justify-content-center form-container" id="request-form">
                
    </select>
            </div>
            <div class="mb-3 question">
                <label for="gender" class="form-label">
                    Mention your Gender
                </label>
                <input type="alphabets" name="gender" class="form-control" placeholder="Gender">
              
                </label>
                <select class="custom-select" name="time">
                    <!--<option selected>Open this select menu</option>
                    <% for (var i=7; i<11; i++) { %>
                        <option value="<%= [i+1] %>:00am">
                            <%= [i+1] %>:00am
                        </option>
                        <% } %>
                            <% for (var i=1; i<12; i++) { %>
                                <option value="<%= [i+1] %>:00pm">
                                    <%= [i+1] %>:00pm
                                </option>
                                <% } %>-->
                
                     </select>
            </div>
            <div class="mb-3 question">
                <label for="days" class="form-label">
                    Please enter the days provided for the order to be customised and delivered
                </label>
                <input type="numbers" name="days" class="form-control" placeholder="Days">
            </div>
            <div class="d-flex justify-content-center">
                <button type="submit" class="btn btn-primary">
                    Submit
                </button>
            </div>
        </form>
    </div>
    </div>
    <script src="/javascript/app/user/request.js"></script>

<%- include("../../footer") -%>